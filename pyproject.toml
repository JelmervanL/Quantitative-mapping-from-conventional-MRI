[project]
name = "qstar"
version = "0.1.0"
description = "Qstar MRI Reconstruction and Analysis"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "torch==2.1.2",
    "pandas==2.2.3",
    "scipy==1.15.2",
    "torchvision==0.16.2",
    "tqdm==4.67.1",
    "wandb==0.19.9",
    "torchio==0.20.6",
    # Dependencies without specific versions requested (will use latest compatible):
    "numpy<2",
    "matplotlib",
    "seaborn",
]

[tool.uv]
# This section is reserved for uv specific configurations if needed later.